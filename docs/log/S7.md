# S7 — E-handel & hemsida

## Built
- 14 articles: cornerstone `ehandel` (written directly, O2 quality bar), then 13 more fanned out as parallel Sonnet subagents per `fable-directs-sonnet-builds` §Fan-out, each reviewed against plan §6 before the one verify run.
- Hub `e-handel` (6): `ehandel`, `dropshipping-sverige`, `webshop`, `konverteringsoptimering`, `starta-webshop`, `basta-e-handelsplattform` (comparison).
- Hub `hemsida` (8): `skapa-hemsida-med-wordpress`, `wordpress-teman`, `basta-webbhotellet` (comparison), `webbhotell`, `doman`, `registrera-doman`, `hemsida-for-foretag-kostnad`, `wordpress-eller-hemsidebyggare`.
- `content/comparisons/basta-e-handelsplattform.ts` (Shopify/Wikinggruppen/Quickbutik/WooCommerce) and `content/comparisons/basta-webbhotellet.ts` (Loopia/One.com/Hostinger/Miss Hosting), 4 rows each.
- `content/image-briefs/s7.json`, one entry per article's image slot (14 entries).
- Every guide keeps a distinct intent from its closest sibling by design: `webbhotell` (what it is, no ranking) vs `basta-webbhotellet` (the ranking); `doman` (concept, choosing a name) vs `registrera-doman` (the registration steps); `ehandel` (legal requirements overview) vs `webshop` (the parts/cost anatomy) vs `starta-webshop` (the ordered launch sequence).

## Decisions
- **`related: []` and body links restricted to hub + tool paths only**, per plan §6 — no article links to another article's slug, even siblings written in this same phase. Cross-article linking is S9's job; this avoids guessing at a related-set that S9 will redo anyway.
- **The `<Stat k="..."/>` component named in plan §6 does not exist** in `src/components/Mdx.tsx` — only `StatRow` (literal values) is registered. Used `StatRow` plus dated inline source links instead, which the common rules already allow as the fallback ("otherwise a source link with date").
- **Legacy slugs for the old "domän", "webshop" and "konverteringsoptimering" posts are best guesses** (`doman`, `webshop`, `konverteringsoptimering`) — `content/legacy-urls.json` is still `"complete": false` and doesn't list them, so plan §4.3's rule (guess from plan §6.4, log it) applies. Anton must confirm the real WordPress paths and, if they differ, rename the file + fix the slug together.
- **`basta-webbhotellet` kept its exact legacy path** since `content/legacy-urls.json` already has a confirmed entry for it.

## Known issues
- Every vendor page (shopify.com, wikinggruppen.se, quickbutik.com, woocommerce.com, loopia.se, one.com, hostinger.se, misshosting.se) and every authority site (Skatteverket, Bolagsverket, Verksamt, Konsumentverket, Internetstiftelsen) is unreachable from this sandbox — same 403 on CONNECT that O1–O3 hit. All comparison-row prices are round approximate figures, not fetched; a human must re-check every price and feature claim in `basta-e-handelsplattform.ts` and `basta-webbhotellet.ts` against the linked vendor page before launch.
- `webshop.mdx` (parts/cost anatomy) and `ehandel.mdx` (legal-requirements overview) cover adjacent ground by design but weren't cross-read against each other during writing — worth a human skim once `starta-webshop` also exists, to confirm the three don't read as redundant.
- Legacy slugs `doman`, `webshop`, `konverteringsoptimering` are unconfirmed guesses (see Decisions) — S9 or Anton should verify against the real WordPress export and fix the filename/slug pair if wrong.
- `hub.featured` in `content/hubs.ts` (O2-owned) still only lists the two O2 exemplars — S7 cannot add its own cornerstone there per §4.9; left for S9.
- Screenshot pass used explicit routes (`tests/screenshots.mjs --fast /ehandel/ /webbhotell/ /basta-webbhotellet/ /starta-webshop/ /doman/`) since the script's default route list is O1/O2-owned and not S7's to edit.

## Verification
Verification: verify green (16 checks) on `phase/S7`; screenshot pass clean at 375 px and 1440 px on `/ehandel/`, `/webbhotell/`, `/basta-webbhotellet/`, `/starta-webshop/`, `/doman/`, no horizontal overflow; screenshots in the PR CI artifact.
