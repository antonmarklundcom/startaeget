# S5 — Ekonomi & jämförelser

## Built
- 8 new guides in `content/articles/ekonomi/`: `bokforing-for-nyborjare` (cornerstone, written directly), `egenavgifter`, `preliminarskatt`, `lon-eller-utdelning-aktiebolag`, `moms-for-nyforetagare`, `bokforing-dropshipping` (legacy), `redovisningsbyra-eller-sjalv`, `foretagskonto`.
- 5 new comparison pages, each an MDX article + `content/comparisons/<slug>.ts`: `basta-foretagsbank` (legacy), `basta-foretagsforsakring`, `basta-kassasystem`, `basta-faktureringsprogram`, `basta-lagerbolag`.
- Re-checked `content/comparisons/basta-bokforingsprogram.ts` per O2's ask; hit the same egress block, values unchanged, comment updated.
- `content/image-briefs/s5.json` — 13 entries, one per new article's hero image.
- Cornerstone written to O2 quality first; the other 12 units fanned out as 4 parallel Sonnet subagents (3 units each), reviewed against plan §6 common rules, then one verify + one screenshot pass + this PR.

## Decisions
- `bokforing-dropshipping` and `basta-foretagsbank` use the slugs plan §6.2 names, not a confirmed legacy path — `legacy-urls.json` is still `"complete": false` and lists both only under `todo_unknown_slugs`. Per this phase's own prompt, used the named slugs and logged it here rather than guessing a WordPress path.
- Reused `fortnox`/`bokio`/`visma-eekonomi` partner ids for lighter product lines they also sell (Fortnox Kassaregister, invoicing modules) instead of inventing new registry entries — `content/affiliates.ts` is out of S5's Owns.
- Added SumUp and Zervant to `basta-kassasystem`/`basta-faktureringsprogram` as plain rows (no `partnerId`, direct `sourceUrl`) so a real competitor isn't dropped just because it has no affiliate program — plan §1.2.
- Every comparison price/freeTier field is honest generic phrasing, not a number, wherever the vendor's own page was unreachable — same convention O2/O3 established.

## Known issues
- **Egress to bank/insurer/kassasystem/lagerbolag/bokföringsprogram vendor pages is still blocked** (confirmed again today) — no comparison row states a specific fee; a human must open each row's `sourceUrl` before launch.
- **`bokforing-dropshipping`** deliberately hedges on import-moms and cross-border EU consumer-VAT specifics (no invented thresholds/scheme names) — flagged in-article with a warning Callout and one `<Verifiera />`; worth a real accountant check before this guide is treated as authoritative.
- **`basta-kassasystem`** describes the kassaregister/kontrollenhet legal requirement only in general terms — exact exemption thresholds not verified (same block).
- Pre-handoff audit found and fixed: two articles (`basta-faktureringsprogram`, `basta-kassasystem`) linked `/artiklar/ekonomi/basta-bokforingsprogram/` instead of the flat `/basta-bokforingsprogram/`; `basta-foretagsbank.ts` row 1 `price` exceeded the 80-char schema limit. Both fixed before this PR.
- The prompt's `<Stat k="...">` component does not exist in `src/components/Mdx.tsx` (only `StatRow`, which takes literal values). Used `StatRow` + inline sourced prose instead, matching O2's own exemplar — worth fixing the wording in future phase prompts.
- `sweden-business-apps` §1/§8 loaded before writing bokföring/moms/lön copy per the prompt; momssatser and the Swedish invoice legal-minimum fields are cited from it in `moms-for-nyforetagare` and `basta-faktureringsprogram`.

## Verification
Verification: verify green (16 checks) on `phase/S5`; screenshot pass clean at 375 px and 1440 px on `/ekonomi/`, `/jamfor/`, `/bokforing-for-nyborjare/`, `/basta-bokforingsprogram/` and `/basta-foretagsbank/`.
