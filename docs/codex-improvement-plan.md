# Updated Codex improvement plan — 2026-09-15

This plan supersedes plan.md's backlog and priority ordering until Anton says otherwise. `plan.md` is kept as-is for historical reference, apart from the requested one-line pointer to this file.

Work order: fix the verification entry point, resolve the documentation against implementation and recorded decisions, review the articles, then record external dependencies. Final validation follows all edits.

1. **DONE — Backlog 1: portable npx spawning.** Changed `scripts/verify.mjs`: all three spawn sites (the shared runner covers build, typecheck and lint, plus the expected-URL loader and production server) use a shell on Windows to resolve `npx.cmd`; Linux keeps direct spawning. Required checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` (results below).
2. **DONE — Backlog 2: affiliate disclosure decision.** Changed only `KNOWN-ISSUES.md`; both components remain unchanged. A plain fallback in a partner slot should retain the visible disclosure under this site's explicit unconditional-marking decision in plan §1.8 and the component comments. Section 1.2 requires affiliate disclosure but does not say that marking must disappear on fallback links. This is the site's transparency policy, not a legal determination that a non-affiliate link requires marking. Filling `affiliateUrl` changes the destination and sponsored relation, not the visible label. Also flagged the contradictory sentence in `content/pages/annonspolicy.mdx`; that file is outside the permitted edit scope.
3. **DONE — Backlog 3: admin persistence.** Changed `KNOWN-ISSUES.md` after reading `src/lib/admin/store.ts` and `github.ts`: missing token or repo selects writable local storage, subject to filesystem permissions. Local changes are not committed to git and are lost when redeployment replaces content from git. Static public pages are not automatically rebuilt by a disk write.
4. **DONE — Backlog 4: article overlap review.** Changed `KNOWN-ISSUES.md` with section-by-section findings for `content/articles/e-handel/{webshop,ehandel,starta-webshop}.mdx`. Significant repetition covers platform, payments, inventory, delivery and legal setup; `ehandel` and `starta-webshop` also nearly duplicate several paragraphs and callouts. Distinct contributions remain (component dependencies/cost structure, EU sales/bookkeeping, ordered launch/testing). Article files remain unchanged. This was a textual overlap review, not tax, legal or price verification.
5. **BLOCKED — Backlog 5: all 22 tax constants.** Not attempted. Anton must supply, for every entry in `src/lib/tax/constants.ts`, the verified value, applicable year/effective date, exact Skatteverket/Bolagsverket/Verksamt source URL, a source excerpt or saved page, and the date checked. Include any corrections and the official tables needed for approximated allowances/credits. Relevant authority sites are confirmed inaccessible from the sandbox in previous phases.
6. **BLOCKED — Backlog 5: legacy URL inventory.** Not attempted. Anton must supply the complete WordPress export or full sitemap/path list for the old site's 48 posts, including original slugs and dated URLs, plus intended destinations for retired pages. Needed to reconcile `content/legacy-urls.json`, resolve guessed slugs and establish completeness.
7. **BLOCKED — Backlog 5: comparison/vendor prices.** Not attempted. Anton must supply verified vendor price data for every row in the six `content/comparisons/` comparisons: vendor/plan, amount, currency, billing period, VAT inclusion, introductory versus renewal price, commitment/eligibility conditions, exact official price URL, date checked and a saved source excerpt/page. Relevant vendor sites are confirmed inaccessible from the sandbox in previous phases.
8. **BLOCKED — Backlog 5: accountant-level VAT review.** Not attempted. Anton must arrange and supply a qualified accountant's written review and corrections for VAT content, including `bokforing-dropshipping`, import VAT, EU consumer distance sales/OSS, thresholds, rates and exceptions, with sources and effective dates. The article overlap review above does not constitute this approval.
9. **BLOCKED — Backlog 5: Hostinger hPanel, DNS and deployment credentials.** Not attempted. Anton must supply/configure the Hostinger Node slot and domain/DNS access, identify the deployment repo/branch and intended DNS targets, configure `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET`, a repo-scoped GitHub token with Contents read/write and `GITHUB_REPO` (plus branch if not `main`) through the hosting secret settings, and confirm the build command/build SHA and autodeploy behavior. Deployment or DNS work needs the corresponding authorized account access or Anton performing those steps. No credential files or deployment configuration were read or changed.
10. **DONE — Plan handover.** Created `docs/codex-improvement-plan.md` and added exactly one pointer line immediately after the first heading in `plan.md`; its remaining content and ordering are preserved.

## Validation

- `npm run lint` — PASS; ESLint reported no errors.
- `npm run typecheck` — PASS; TypeScript reported no errors.
- `npm test` — PASS; 67 tests across 3 files passed.
- `npm run build` — PASS; Next.js production build completed and generated 90 pages.
- Windows spawn smoke checks — PASS for both `spawnSync` and `spawn` running `npx next --version` with the script's platform-specific shell option. All three spawning sites carry the option. Linux was not executed here; its existing direct-spawn behavior is preserved.
- Additional expected-URL smoke check — FAIL: `npx tsx scripts/expected-urls.ts` launched successfully but tsx failed in `os.userInfo()` with `uv_os_get_passwd` / ENOMEM in this sandbox. No full runtime verification claim is made. This does not replace any of the four required checks above.
- Node 24 emits DEP0190 for asynchronous spawning with shell and args; the script passes fixed tool arguments and a numeric port, not user-supplied command text.
- Scope check — PASS; only the four permitted files changed. `plan.md` matches its original content after removing exactly the added second line (ignoring checkout line-ending normalization). `git diff --check` passed.

## Addendum — 2026-09-16: vendor price verification, first pass

Backlog 5 item "comparison/vendor prices" (entry 7 above) is no longer fully BLOCKED. Two of the eight comparison files were verified by loading each vendor's own pricing page directly, and their rows were corrected: `content/comparisons/basta-bokforingsprogram.ts` and `content/comparisons/basta-webbhotellet.ts`. The remaining six (`basta-e-handelsplattform`, `basta-faktureringsprogram`, `basta-foretagsbank`, `basta-foretagsforsakring`, `basta-kassasystem`, `basta-lagerbolag`) were not touched and still need the same treatment.

11. **OPEN — Björn Lundén may have rebranded to "Lundify"; do not rename until confirmed against the vendor.** Multiple independent third-party Swedish comparison sites state that Björn Lundén's bokföringsprogram is now marketed as "Lundify", and bjornlunden.se now routes its software/product line to bjornlunden.com under a new visual identity. This could **not** be confirmed from the vendor's own current product pages — the name "Lundify" was not found on a vendor-controlled page during either the 2026-09-16 manager browsing pass or the follow-up pass. Because of that, nothing was changed: the `bjorn-lunden` row in `content/comparisons/basta-bokforingsprogram.ts` keeps its name, its price ("Från ca 200 kr/mån beroende på paket") and its 2026-09-11 `sourceDate`, and the `bjorn-lunden` entry in `content/affiliates.ts` is untouched. **Action needed:** Anton or a future session must open bjornlunden.com's actual software product pages (not the corporate homepage) and establish (a) the current product name, (b) the current list price with billing period and VAT treatment, and (c) a stable official price URL. Only then rename the partner's display name — and, as with `visma-eekonomi`, keep the partner `id` unchanged because `/go/bjorn-lunden/` is a stable public URL.

12. **OPEN — Wint price is still unverified.** `https://www.wint.se/priser` returned HTTP 403 on 2026-09-16, so the `wint` row keeps its qualitative price text ("Abonnemang efter bolagets storlek") and its 2026-09-11 `sourceDate`. A third-party source suggests a range of roughly 625–3 290 kr/mån; that was not used because it is not vendor-confirmed. Needs a retry from a context Wint does not block.

13. **OPEN — "Visma eEkonomi" still appears in three articles whose comparison tables were out of scope.** The rebrand to Spiris is vendor-confirmed (spiris.se: "Bokföringsprogrammet Visma eEkonomi har bytt namn till Spiris", and `vismaspcs.se/produkter/bokforingsprogram` now 301s to `spiris.se`). It was applied to `content/affiliates.ts`, `content/comparisons/basta-bokforingsprogram.ts` and `content/articles/ekonomi/basta-bokforingsprogram.mdx`. It was deliberately **not** applied to `content/articles/ekonomi/basta-faktureringsprogram.mdx`, `content/articles/ekonomi/basta-foretagsbank.mdx` or `content/articles/ekonomi/foretagskonto.mdx`, because `content/comparisons/basta-faktureringsprogram.ts` was out of scope for this pass and renaming the prose while that table still says "Visma eEkonomi" would contradict itself on the same page. Do the faktureringsprogram comparison file and those three articles together in one pass.
